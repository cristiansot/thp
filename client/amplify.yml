version: 1
applications:
  - appRoot: client
    backend:
      phases:
        preBuild:
          commands:
            - echo "Desactivando integración con SSM"
        build:
          commands:
            - echo "Construyendo sin secretos"
    frontend:
      phases:
        preBuild:
          commands:
            - echo "Desactivando integración con secretos"
        build:
          commands:
            - echo "Construyendo frontend"

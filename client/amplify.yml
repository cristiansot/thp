version: 1
applications:
  - appRoot: client 
    frontend:
      phases:
        build:
          commands:
            - npm install
            - npm run build
      artifacts:
        baseDirectory: client/dist  
        files:
          - '**/*'

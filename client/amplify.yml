version: 1
env:
  variables:
    NODE_VERSION: 20
applications:
  - appRoot: client
    frontend:
      phases:
        preBuild:
          commands:
            - nvm install 20
            - npm install
        build:
          commands:
            - npm run build
      artifacts:
        baseDirectory: dist
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*

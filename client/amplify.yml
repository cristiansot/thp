version: 1
application:
  - appRoot: /client
    frontend:
      phases:
        build:
          commands:
            - echo "Skipping build..."
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
